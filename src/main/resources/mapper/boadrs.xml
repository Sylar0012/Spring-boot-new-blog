<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.red.domain.boards.BoardsDao">
	
	<select id="findByUsername"
		resultType="site.metacoding.red.domain.users.Users">
		SELECT * FROM users WHERE username=#{username}
	</select>
	
	<update id ="UserDelete">
		UPDATE boards SET usersId = null WHERE usersId =#{id}
	</update>

	
	<insert id="insert">
		INSERT INTO users(username, password, email, createdAt) 
		values(#{username}, #{password},#{email}, NOW())
	</insert>

	<select id="findById" resultType="site.metacoding.red.domain.users.Users">
		SELECT * FROM users WHERE id = #{id}
	</select>
	
	<update id="update">
		UPDATE users SET password = #{password}, email =#{email} WHERE id = #{id}
	</update>
	
	<select id="usernameCheck" resultType="site.metacoding.red.domain.users.Users">
		SELECT * FROM users WHERE username = #{username}
	</select>
	
	<delete id="deleteById">
		DELETE FROM users WHERE id = #{id}
	</delete>
</mapper>